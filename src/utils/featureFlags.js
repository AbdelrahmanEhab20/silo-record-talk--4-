/**
 * Feature flags for phased rollout (standalone / enterprise deployment).
 * Turn on when backend OAuth and sync are ready.
 */
export const FEATURES = {
  /** Google Calendar (Outlook still pending) */
  calendarIntegrations: true,
};
